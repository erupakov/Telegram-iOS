# Firebase iOS SDK is fetched as an external repo by Bazel — see //:MODULE.bazel
# (http_archive "firebase_ios_sdk" using //submodules/DivoFirebaseSDK:firebase.BUILD).
# Targets live at @firebase_ios_sdk//:FirebaseCore, etc.

exports_files(["firebase.BUILD"])
